#include <stdio.h>

int main() {
    // Always declare variables before using them in C.
    int some_variable = 50;
    printf("The value of some_variable is: %d\n", some_variable);
    // The %d format specifier is used to print an integer value.
    // The value of some_variable is printed to the console.

    return 0;
}